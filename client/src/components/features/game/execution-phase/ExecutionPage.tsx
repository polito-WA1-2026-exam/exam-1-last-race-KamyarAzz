export default function ExecutionPage() {
  // save the destination and start in server
  // send to client by API call
  // send the selected routes
  // save the selected routes in server
  // resend the selected routes and events (skip if invalid routes)
  // show results and save to leaderboard

  return <div>ExecutionPage</div>;
}
